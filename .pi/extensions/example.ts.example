// This file is not auto-loaded. Copy it to a uniquely named `.ts` file and
// uncomment the code only when the project needs a repo-local Pi extension.
//
// import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";
//
// export default function projectExtension(pi: ExtensionAPI) {
//   pi.registerCommand("project-status", {
//     description: "Show a small project-specific status message.",
//     handler: async (_args, ctx) => {
//       if (ctx.hasUI) ctx.ui.notify("Project extension is active.", "info");
//     },
//   });
// }
