# Pre-commit fence. Cheating with --no-verify is blocked by agent hooks.
# Install: pnpm exec lefthook install (also via prepare)
pre-commit:
  parallel: false
  commands:
    check:
      run: pnpm check
    test:
      run: pnpm test
