---
title: "Worker task: research-effect-mcp-over-http"
type: "task"
status: "active"
created_at: "2026-09-21"
privacy: "public"
---

# Worker task: research-effect-mcp-over-http

Effort brief: `.brain/projects/ratstack-sh/ratstack-sh-brief.svx` (read it first; its Vocabulary binds you).
Your step: `.brain/projects/ratstack-sh/research-effect-mcp-over-http.svx` (already claimed for you).

## What to do

From `.agent_sources/github.com/Effect-TS/effect/packages/effect/src/unstable/ai/McpServer.ts`, `McpProtocol.ts`, `McpSchema.ts`, and their tests: record `McpServer.layerHttp` options and the `HttpRouter` it needs, the protocol adapters to pass, whether sessions are stateless enough for a Worker (no shared memory between requests), how `McpServer.resource` (static and templated) and `McpServer.prompt` are declared and listed, how `instructions` and `websiteUrl` surface, how the existing `packages/capability/test/mcp-harness.ts` drives a server in memory, and what an MCP server card (`/.well-known/mcp/server-card.json`) must say for this server. Also record how a `skills/<name>/SKILL.md` file becomes a prompt (frontmatter to prompt name and description; body to one user message) and how the same file is served at `/skills/<name>` as text.

Resolved when `research-effect-mcp-over-http-asset.svx` exists with a working in-memory example (resource, prompt, tool, listed and read through the harness) and the server card JSON.

## Deliverables (file edits only)

1. Write the asset `.brain/projects/ratstack-sh/research-effect-mcp-over-http-asset.svx` with frontmatter `title`, `type: "asset"`, `parent: "./research-effect-mcp-over-http.svx"`, `created_at: "2026-09-21"`, `privacy: "public"`, then plain Markdown. Cite file paths in `.agent_sources/` or URLs for every claim; mark anything unverified as such.
2. In `.brain/projects/ratstack-sh/research-effect-mcp-over-http.svx`: add a `## Result (2026-09-21)` section with the gist and a link to the asset, and set `status: "closed"` plus `closed_at: "2026-09-21"` in the frontmatter.
3. Do not edit the brief. Do not touch files outside `.brain/`. Do not run `pnpm install`, deploy anything, or write outside this repo except scratch under `/tmp`.

Work autonomously, do not commit, print a DONE summary with the asset path.
