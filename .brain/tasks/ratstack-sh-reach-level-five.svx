---
title: "Worker task: reach-level-five"
type: "task"
status: "active"
created_at: "2026-09-22"
privacy: "public"
---

# Worker task: reach-level-five

You work in a rift (a full copy of the repo) at `/Users/joel/Code/.worktrees/joelhooks/rat-stack/level-five` on branch `worker/level-five`; every path below is relative to it. Another worker edits the main checkout in parallel; never touch `/Users/joel/Code/joelhooks/rat-stack`.

Effort brief: `.brain/projects/ratstack-sh/ratstack-sh-brief.svx` (read it first; Vocabulary and Decision ledger bind you). Decisions that bind this step: `decide-not-applicable-checks.svx`. Evidence: `research-agent-ready-checks-asset.svx` (the exact probes and minimal fixtures per check), `research-alchemy-effect-worker-asset.svx` (DNS record declaration shape, zone adoption). The site is live at https://ratstack.sh (v0.1); `deploy-ratstack-sh.svx` says what is verified.
Your step: `.brain/projects/ratstack-sh/reach-level-five.svx` (already claimed for you). You do the build half only.

## What to do

Run `POST https://isitagentready.com/api/scan` with `format: "agent"` against `https://ratstack.sh`, fix failing applicable checks in `apps/mischief` (paths, headers, Link headers, well-known documents), redeploy, rescan; stop at Level 5 or when the only failures are the not-applicable set with their documented treatment.

Build half (worker, in the `level-five` rift, no deploy): from the [not-applicable decision](./decide-not-applicable-checks.svx) and the [checks asset](./research-agent-ready-checks-asset.svx) rows for `a2aAgentCard`, `ard`, `authMd`, `dnsAid`, `webBotAuth`: a minimal real A2A endpoint at `/a2a` (JSON-RPC over HTTP per the A2A spec, one skill that answers a question with the same `search` and `read` capabilities) with its agent card at every path the scanner tries; the ARD manifest; an honest `/auth.md` (H1 contains `auth.md`; access is anonymous; no registration; no OAuth server); DNS-AID SVCB and TXT records declared in the Stack under `_agents` with `Cloudflare.DNS.Record`, and whatever Alchemy offers for DNSSEC on the adopted zone (if nothing, say so and the owner enables it once); a Web Bot Auth key directory behind a flag that defaults off, with the key read from a Worker secret binding, ready if `auth.md` does not count. Each document tested over `toWebHandler` against the asset's probes.

Scan half (owner): deploy, run `pnpm mischief:smoke`, save the scan output as `reach-level-five-asset.svx`, flip the Web Bot Auth flag only if needed, repeat.

Resolved when the smoke script prints Level 5 and the scan output is saved as an asset.

## Write scope

`apps/mischief` (not `public/`), `apps/infra/alchemy.run.ts` (DNS records; keep the zone adoption and the Worker as they are), `.brain/`. No new dependency versions. Do not deploy, do not authenticate with Cloudflare, do not commit, do not edit the brief. The A2A spec and the ARD, auth.md, DNS-AID, and Web Bot Auth skill pages at `https://isitagentready.com/.well-known/agent-skills/<name>/SKILL.md` are fair to fetch.

## Deliverables

- The work, `pnpm turbo run check test build` green in the rift (run `pnpm fix` first when formatting complains; never loosen the fence).
- In the step file: `## Build result (2026-09-22)` with what exists, each document's path, the DNS records declared, the DNSSEC answer, how each was tested, and what felt forced. Leave `status: "open"`; the owner closes it after the scan.
- Print a DONE summary with the file list and the gate result.
